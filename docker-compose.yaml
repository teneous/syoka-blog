services:
  syoka-blog:
    image: docker.io/teneous/syoka-blog:latest
    pull_policy: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
    restart: unless-stopped
